# CompilerAndInterpreter
A simple educational compiler and interpreter for a custom functional language. The project includes lexical analysis, parsing, AST generation, bytecode compilation, and dynamic type checking via an interpreter.
